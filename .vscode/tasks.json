{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Env: Sync config.json from CDK outputs",
      "type": "shell",
      "command": "scripts/sync-env.sh"
    },
    {
      "label": "Cleanup: Remove LLD Layer",
      "type": "shell",
      "command": "SKIP_NEXT_BUILD=1 npx lld --remove all"
    }
  ]
}
