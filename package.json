{
  "name": "crs-sst-cdk",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "cdk": "cdk",
    "synth": "cdk synth",
    "deploy": "cdk deploy --all && scripts/sync-env.sh",
    "diff": "cdk diff",
    "lint": "biome check .",
    "lint:fix": "biome check --fix .",
    "format": "biome format --write .",
    "debug": "lld",
    "dev": "pnpm --filter web dev",
    "dev:debug": "pnpm run dev & pnpm run debug",
    "env:sync": "scripts/sync-env.sh"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.10",
    "aws-cdk": "2.1116.0",
    "lambda-live-debugger": "^1.12.0",
    "typescript": "~5.9.3"
  }
}
